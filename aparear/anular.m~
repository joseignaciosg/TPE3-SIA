%a y b son los individuos que se reciben como parametro
function [a, b] = anular(a, b)

l = length(V(:,1));
start = ceil( rand * l );
L = ceil( rand * l/2 );
i = 0;
while ( i <= L )

	aux = a(mod(start + i, l));
	a((start + i, l)) = b((start + i, l));
	b((start + i, l)) = aux;

	i = i + 1;
end

end
